---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault-data-claim
  namespace: vault
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: vault-data-volume